{% extends "base.html.twig" %}

{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit">Ajouter un programme :D</button>
	{{ form_end(form) }}

	<br><br>
	<a href="{{ path('list_index') }}">
		Retour à la liste
	</a>
{% endblock %}
