{% extends 'base.html.twig' %}

{% block title %}All programs
{% endblock %}

{% block body %}
	<h1>Toutes les séries de la table program :
	</h1>
	{% for program in programs %}
		<div>
			<h2>
				{{ loop.index }}
				/
				{{ program.title }}
				- Catégorie :
				{% if program.category is not null %}
					{{ program.category.name }}
				{% else %}
					Catégorie non définie
				{% endif %}
			</h2>
			<p>{{ program.synopsis }}</p>
			{# Lien vers la fiche de la série : #}
			<a href="{{ path("program_show", {id :program.id }) }}">Voir la fiche</a>
		</div>
	{% else %}
		Aucune série trouvée.
	{% endfor %}
	<br>
	<br>
	Pas assez de programmes ?? Pas de panique ! Ajoute ton programme:
	<a href="{{ path('new_program') }}">Ajouter son programme</a>
	<br>
	<br>
	<a href="{{ path('accueil_index') }}">
		Retour à l'accueil
	</a>
{% endblock %}
